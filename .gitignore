# Rust
/target
**/*.rs.bk

# Node
node_modules/
packages/*/dist/

# build outputs (regenerated in CI / by `npm run build`)
# NOTE: binding.js + binding.d.ts (the loader) ARE committed — needed by the
# publish job's TypeScript build and shipped in the package.
packages/*/*.node
packages/*/npm/*/*.node
packages/*/wasm/

# misc
*.log
.DS_Store
artifacts/

# Claude Code local config
.claude/
